apply plugin: 'java'

sourceCompatibility = 1.7
version = '1.0'

repositories {
    mavenCentral()
}

buildscript {
    repositories {
        jcenter()
    }

}

dependencies {
    testCompile 'junit:junit:4.11'
}

task stage(dependsOn: [assemble])
